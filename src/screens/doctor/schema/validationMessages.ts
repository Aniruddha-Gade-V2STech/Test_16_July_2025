const ERR = {
  remarks: {
    required: 'Please enter your reject reason.',
    min: 'Reject reason must be at least 4 characters.',
    max: 'Reject reason must be at most 50 characters.',
  },
};

export default ERR;
